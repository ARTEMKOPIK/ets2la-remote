<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ backup / Device-to-Device transfer rules. We exclude
    SharedPreferences and app files because they can contain saved PC
    hosts and MAC addresses, which shouldn't leave the device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
